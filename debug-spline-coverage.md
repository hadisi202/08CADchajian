# debug-spline-coverage.md

- Status: OPEN
- SessionId: spline-coverage
- Goal: 定位为什么当前样条实体尺寸计算仍不准确，并对单弧 / 双弧 / 三段连续弧 / 样条+圆弧混合轮廓做专项回归，输出支持清单与未覆盖边界。

## Hypotheses
- H1: `ComputeDualSplineMidlineLength()` 只尝试样条双轨的同向/反向归一化配对，遇到局部曲率变化或样条参数分布不均时，中线被低估或高估。
- H2: `SampleRailDistance()` 对样条和样条+圆弧混合轮廓仍使用最近点采样，局部最近点会跨越到错误分支，导致轨距和厚度识别失真。
- H3: 连续轮廓在 `TryCompositeRailPath()` 中虽然能识别多段曲线，但样条段与直线段、圆弧段之间的边链归属传播不稳定，造成外/中/内累计错位。
- H4: 三轴截面候选当前按 `cand.Length` 选最大值，对样条或混合轮廓会误选错误方位截面，所以同一实体换方位后结果差异较大。
- H5: 当前已修好的相连圆弧路径只覆盖了解析圆弧链，对样条+圆弧混合链缺少同等级的长度一致性校验与回写验证。

## Evidence Plan
- 对样条、圆弧、混合轮廓分别输出：轴向候选结果、主曲线对、附加曲线对、轨距、外/中/内长度、最终回填值。
- 对样条双轨额外输出：正向中线、反向中线、选中结果，判断是否存在配对错位。
- 对专项回归样本保留 pre-fix / post-fix 日志，形成支持清单、风险边界和未覆盖场景。
