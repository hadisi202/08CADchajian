# 家具参数化建模插件用户手册

## 概述

本插件是一个基于AutoCAD的家具参数化建模工具，提供了丰富的板件创建、编辑和管理功能。

## 主要功能命令

### 板件信息管理

#### SETI - 设置板件信息
- **功能**: 为选中的3D实体设置详细的板件信息
- **使用方法**: 输入`SETI`命令，选择板件实体，在弹出的窗口中设置相关参数
- **参数包括**: 订单号、柜号、房间、板件名称、材质、纹路方向、尺寸信息等
- **更新说明（2026-05）**:
  - `显示纹路线/隐藏纹路线` 按钮改为直接作用于当前板件，点击后立即生效；
  - 纹路显示状态会持久化保存，下次读取板件信息仍保持一致；
  - SETI 界面已移除五金区显示。

#### PLXG - 批量修改板件信息
- **功能**: 批量修改多个板件的信息
- **使用方法**: 输入`PLXG`命令，选择要修改的板件，在批量编辑窗口中进行修改
- **更新说明（2026-05）**:
  - 修复 `全选/取消全选/撤销` 按钮；
  - 修复“拾取板件信息”后自动识别并勾选已赋值字段；
  - 移除“圆弧板件”与底部无效勾选控件。

#### CKLB - 查看板件信息列表
- **功能**: 显示当前图纸中所有板件的详细信息列表
- **使用方法**: 输入`CKLB`命令，查看板件统计信息和详细列表
- **更新说明（2026-05）**:
  - 批量修改形状后，列表会同步显示最新形状；
  - 形状数据保存/反序列化已修复，避免被错误回退为默认值。

## 测试用例（2026-05）

### 单元测试
- 工程路径：`d:\cursor\08CADchajian\FurniturePlugin.Tests`
- 执行命令：`dotnet test d:\cursor\08CADchajian\FurniturePlugin.Tests\FurniturePlugin.Tests.csproj --configuration Debug`
- 覆盖点：
  - `PanelInfo` 纹路状态序列化/反序列化一致性；
  - `PLXG` 自动勾选规则；
  - 形状文本与计算类型映射规则。

### 回归测试
- 主项目编译：`dotnet build --configuration Debug` 通过（0错误/0警告）。
- 功能回归项：
  - `SETI` 纹路显示/隐藏即时生效与持久化；
  - `PLXG` 全选/取消全选/撤销与自动勾选；
  - `CKBJ` 形状批量修改后实时刷新。

### 数据导出

#### ExportPanelInfoToCsv - 导出板件信息到CSV
- **功能**: 将板件信息导出为CSV格式文件
- **使用方法**: 输入命令后选择保存位置

### 板件创建

#### CJBJ - 快速创建板件
- **功能**: 通过参数快速创建标准板件
- **使用方法**: 输入`CJBJ`命令，在窗口中设置板件尺寸和参数

#### ZYCEBAN - 左右侧板参数化建模
- **功能**: 创建左右侧板的参数化模型
- **使用方法**: 输入`ZYCEBAN`命令，设置柜体参数

#### DINDIBAN - 顶底板参数化建模
- **功能**: 创建顶底板的参数化模型
- **使用方法**: 输入`DINDIBAN`命令，设置柜体参数

#### CENGBAN - 层板参数化建模
- **功能**: 创建层板的参数化模型
- **使用方法**: 输入`CENGBAN`命令，设置层板参数

#### BEIBAN - 背板参数化建模
- **功能**: 创建背板的参数化模型
- **使用方法**: 输入`BEIBAN`命令，设置背板参数

#### KJ - 智能外框架创建
- **功能**: 智能化的外框架创建工具，通过选择现有板件或框选区域自动识别尺寸，避免重复输入参数
- **核心特点**:
  - **智能尺寸识别**: 通过选择现有板件自动推算柜体尺寸
  - **框选区域识别**: 框选包含板件的区域，自动分析框架尺寸
  - **外框架优先**: 先创建参数化外框架，再根据框架创建内部板件
  - **自动计算**: 根据盖法自动计算各板件的精确尺寸
  - **参数继承**: 新板件自动继承框架的材质和厚度参数
- **使用方法**:
  1. 输入`KJ`命令打开智能外框架界面
  2. 选择识别方式：
     - **选择板件识别**: 点击"选择板件"按钮，选择现有板件自动推算尺寸
     - **框选区域识别**: 点击"框选区域"按钮，框选包含板件的区域进行分析
     - **手动输入**: 直接输入柜体的宽度、高度、深度参数
  3. 设置板件参数：
     - 侧板厚度、顶底板厚度
     - 材质选择
     - 盖法选择（侧板盖顶底板 / 顶底板盖侧板）
  4. 选择创建选项：
     - 创建左右侧板
     - 创建顶底板
  5. 点击"创建框架"按钮在指定位置创建板件
- **智能识别算法**:
  - **板件特征分析**: 根据长宽高比例自动判断板件类型（侧板/顶底板）
  - **尺寸推算**: 基于选中板件的尺寸和类型推算整体柜体尺寸
  - **区域分析**: 分析框选区域内所有板件的分布和尺寸关系
  - **参数验证**: 自动验证推算结果的合理性
- **应用场景**:
  - 基于现有板件快速创建配套板件
  - 批量创建标准化柜体框架
  - 复制现有柜体的尺寸规格
  - 参数化建模的起始框架创建

### 查找和分析

#### CZBJ - 通过ID查找板件
- **功能**: 根据板件ID快速定位和查看板件信息
- **使用方法**: 输入`CZBJ`命令，输入要查找的板件ID

#### OBB - 计算有向包围盒尺寸
- **功能**: 计算选中实体的有向包围盒(OBB)尺寸
- **使用方法**: 输入`OBB`命令，选择要分析的3D实体

### 布尔运算

#### SUU - 分别差集运算
- **功能**: 对第一次选择的每个实体分别与第二次选择的实体进行差集运算
- **特点**: 与AutoCAD原生SUBTRACT命令不同，此命令不会合并第一次选择的实体
- **使用方法**: 
  1. 输入`SUU`命令
  2. 选择要从中减去的实体（可多选）
  3. 选择要减去的实体（可多选）
  4. 系统会对第一次选择的每个实体分别执行差集运算
- **应用场景**: 需要保持多个实体独立性的差集运算，如批量开孔、切割等操作

#### BOO - 智能内空识别与封闭
- **功能**: 使用先进的边界追踪算法，自动识别框选区域内由多条交叉线条围成的所有内空区域，并创建封闭边界或面域
- **核心特点**: 
  - **精确边界追踪**: 使用先进的边界追踪算法，能够准确识别复杂的内空形状
  - **凸包计算**: 采用 Graham 扫描算法计算区域的凸包，确保边界的准确性
  - **洪水填充**: 使用洪水填充算法进行连通性分析，自动分离不同的内空区域
  - **高密度采样**: 使用 100x100 网格进行高精度采样，提高识别准确性
  - **多类型支持**: 支持直线、圆弧、圆、多段线等各种曲线类型
  - **智能优化**: 自动优化边界形状，使其更贴合实际的线条轮廓
- **使用方法**: 
  1. 输入`BOO`命令
  2. 选择创建类型：
     - 输入`B`或直接回车：创建封闭边界
     - 输入`R`：创建面域
  3. 框选包含交叉线条的区域
  4. 系统自动识别所有内空区域并创建相应的边界或面域
- **支持的对象类型**: 
  - 直线段(LINE)
  - 多段线(LWPOLYLINE, POLYLINE)
  - 圆(CIRCLE)
  - 椭圆(ELLIPSE)
  - 圆弧(ARC)
  - 样条曲线(SPLINE)
- **技术实现**:
  - **边界追踪算法**: 使用网格采样结合射线法进行内外判断
  - **凸包计算**: Graham 扫描算法确保边界的几何正确性
  - **连通性分析**: 洪水填充算法自动分离不同区域
  - **射线相交检测**: 精确计算射线与各种曲线类型的交点
  - **角度范围检查**: 准确处理圆弧的角度范围限制
- **算法特性**:
  - **射线相交检测**: 精确计算射线与各种曲线类型的交点
  - **内外判断**: 使用奇偶规则判断点是否在封闭区域内部
  - **区域分组**: 智能将相邻的内部点分组为连续区域
  - **边界创建**: 为每个内空区域创建精确的边界轮廓
  - **颜色区分**: 不同内空区域用不同颜色显示便于识别
- **应用场景**: 
  - 复杂交叉图形的内空区域自动提取
  - 建筑平面图中房间区域的自动识别
  - 机械图纸中孔洞和空腔的批量处理
  - 电路图中封闭区域的自动标识
  - 任何需要识别内部封闭空间的场景

### 材质和显示

#### TESTMAT - 测试材质显示
- **功能**: 测试和预览材质效果
- **使用方法**: 输入`TESTMAT`命令

#### TESTMATLIB - 打开材质库
- **功能**: 打开AutoCAD材质库进行材质管理
- **使用方法**: 输入`TESTMATLIB`命令

#### ShowAllTextureDirections - 显示所有纹理方向
- **功能**: 显示当前图纸中所有板件的纹理方向信息
- **使用方法**: 输入命令查看纹理方向统计

## 功能特性

### 数据持久化
- 板件信息自动保存到AutoCAD图纸中
- 支持图纸重新打开后数据恢复
- 提供数据验证和错误处理机制

### 智能识别
- 自动识别3D实体的几何特征
- 智能计算板件尺寸和方向
- 支持AABB和OBB两种尺寸计算方法

### 用户界面
- 提供直观的WPF界面
- 支持实时预览和参数调整
- 集成材质选择和纹理方向设置

### 批量处理
- 支持批量选择和编辑
- 提供批量导出功能
- 优化的性能处理机制

## 使用步骤

1. **启动插件**: 加载插件后会显示欢迎信息和可用命令列表
2. **创建或选择板件**: 使用参数化建模命令创建板件，或选择现有的3D实体
3. **设置板件信息**: 使用SETI命令为板件设置详细信息
4. **管理和编辑**: 使用PLXG进行批量修改，CKLB查看列表
5. **导出数据**: 使用导出命令将数据保存为外部文件

## 注意事项

- 确保AutoCAD版本兼容性（支持2021-2025版本）
- 建议在操作前备份重要图纸
- 大批量操作时注意性能影响
- 定期使用测试命令验证插件功能

## 技术支持

如遇到问题，可以：
1. 查看AutoCAD命令行的错误信息
2. 检查插件依赖文件是否完整
